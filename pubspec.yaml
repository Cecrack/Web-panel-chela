name: chela_medic_web
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.7.2 # Asegúrate de que tu SDK sea compatible con la versión de intl que elijas.

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.2.1 # Asegúrate de usar la última versión
  cupertino_icons: ^1.0.8
  rxdart: ^0.28.0

  # Para formateo de fechas y números
  intl: ^0.19.0 # <--- AÑADE ESTA LÍNEA

  # Firebase core & servicios
  firebase_core: ^2.25.0
  firebase_auth: ^4.17.4
  cloud_firestore: ^4.17.5

  # Navegación web moderna
  go_router: ^13.0.0

  # PDF
  pdf: ^3.10.4
  printing: ^5.11.1

  badges: ^3.1.1 # Añade esta línea (o la versión más reciente)

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true